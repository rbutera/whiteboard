{
  "name": "@wboard-examples/kanban",
  "version": "0.0.0",
  "private": true,
  "description": "Worked example: the whiteboard library path — BoardService + project, driven with plain asserts.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/main.js",
  "dependencies": {
    "@wboard/core": "workspace:*",
    "@wboard/server": "workspace:*"
  },
  "devDependencies": {
    "zod": "^4.4.3"
  }
}
