{
  "schema": {
    "kinds": [
      {
        "id": "note",
        "description": "a sticky note",
        "attributes": [
          { "name": "text", "description": "the note body", "type": "string", "required": true }
        ]
      }
    ]
  },
  "input": {
    "ops": [{ "op": "update", "op_id": "o1", "id": "ghost", "data": { "text": "nobody home" } }]
  },
  "expect": { "reject": "unknown-element" }
}
