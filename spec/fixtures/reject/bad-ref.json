{
  "schema": {
    "kinds": [
      {
        "id": "note",
        "description": "a sticky note",
        "attributes": [
          { "name": "text", "description": "the note body", "type": "string", "required": true },
          {
            "name": "parent",
            "description": "the parent element",
            "type": "element",
            "required": false
          }
        ]
      }
    ]
  },
  "input": {
    "ops": [
      {
        "op": "create",
        "op_id": "o1",
        "element": { "id": "e1", "kind": "note", "data": { "text": "orphan", "parent": "ghost" } }
      }
    ]
  },
  "expect": { "reject": "bad-ref" }
}
